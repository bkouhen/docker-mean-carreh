version: 0.0
os: linux
files:
  - source: ./test.sh
    destination: /var/app
  - source: ./docker-compose.yml
    destination: /var/app
  - source: ./docker-compose.prod.yml
    destination: /var/app

hooks:
  AfterInstall:
    - location: /var/app/test.sh
      timeout: 300
      runas: root