version: 0.0
os: linux
files:
  - source: ./launch_app_ec2.sh
    destination: /var/app
  - source: ./docker-compose.yml
    destination: /var/app
  - source: ./docker-compose.prod.yml
    destination: /var/app

hooks:
  AfterInstall:
    - location: launch_app_ec2.sh
      timeout: 300
      runas: root